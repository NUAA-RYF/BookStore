<?xml version="1.0" encoding="utf-8"?>

<androidx.core.widget.NestedScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".fragment.BookRecommendFragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bootstrap_gray_lighter"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/bootstrap_gray_lighter">

        <include
            android:id="@+id/recommend_header"
            layout="@layout/head_item"/>

        <com.youth.banner.Banner
            android:id="@+id/book_recommend_banner"
            android:layout_width="match_parent"
            android:layout_height="150dp"
            android:layout_marginHorizontal="8dp"
            android:layout_marginVertical="10dp"
            app:image_scale_type="fit_center"
            app:layout_constraintTop_toBottomOf="@id/recommend_header"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/book_recommend_banner"
            app:layout_constraintLeft_toLeftOf="@id/book_recommend_banner"
            app:layout_constraintRight_toRightOf="@id/book_recommend_banner"
            tools:itemCount="5"
            />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.core.widget.NestedScrollView>
